sudo: false

language: c

compiler:
  - gcc

addons:
  apt:
    packages:
      - gfortran
      - libblas-dev
      - liblapack-dev
      - libopenmpi-dev
      - openmpi-bin

script:
  - ./bootstrap
  - ./configure --enable-mpi
  - make all
  - make check
  - make distcheck
